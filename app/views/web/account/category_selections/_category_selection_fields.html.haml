.nested-fields.form-group.well
  = f.association :category, collection: categories_for_select, include_blank: false, label: false
  = f.input :kind, as: :radio_buttons, label: false
  = link_to_remove_association "remove", f, class: 'btn btn-danger'
